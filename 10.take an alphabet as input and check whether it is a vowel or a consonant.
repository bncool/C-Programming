#include <stdio.h>

int main () {
	
	char ch;
	printf("\n enter a character");
	scanf ("%c",&ch);
	if (ch=='a' || ch=='i' || ch == 'e' || ch =='o' || ch == 'u' )
	{
		printf("it is a vowel");
	}
	
	else { 
	printf("it is a cons");
	}
	
	
	return 0;
}
