#include <stdio.h>
#include <math.h>
int main () {
	int a,b,c; 
	printf("enter any three number");
	scanf("%d%d%d",&a,&b,&c);
	if ( a==b || b==c || a ==c) {
		
		
		printf("this is a isosceles triangle");
	}
	
	else {printf("this is not a isosceles triangle");
	}
	
	
	return 0;
}
