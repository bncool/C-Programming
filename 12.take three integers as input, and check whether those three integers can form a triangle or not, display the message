#include <stdio.h>


int main() {
 int a,b,c;
 printf("enter any value of a,b and c");
 scanf("%d%d%d",&a,&b,&c);
 if (a+b>c && b+c>a && a+c>b) {printf("triangle is formed");
 
 }
 else {printf("triangle is not formed");
 }
return 0;
}
